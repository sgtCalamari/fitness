const exerciseType = require('./exerciseType.model');
const muscleGroup = require('./muscleGroup.model');
const user = require('./user.model');
const workout = require('./workout.model');

module.exports = {
  exerciseType,
  muscleGroup,
  user,
  workout
};
